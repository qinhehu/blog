var settings = {
  database: {
    protocol: "mysql",
    query: {
      pool: true
    },

    // host: "localhost",
    // port: '3306',
    // database: "blog",
    // user: "root",
    // password: "123456"
  }
};

module.exports = settings;
